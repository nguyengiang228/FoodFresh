import Box from "@mui/material/Box";
import { useParams } from "react-router-dom";
import { useGetProductByIdQuery } from "~/redux/api/api.caller";
// import { useGetProductItemQuery } from "~/redux/api/api.caller";

const ChildPage = () => {
  // const Ids = useParams();
  // console.log("id:", Ids);
  // const { data } = useGetProductByIdQuery(+Ids);
  return <Box>ChildPageaaaaaaaaaaaa</Box>;
};

export default ChildPage;
